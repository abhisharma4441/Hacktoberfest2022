#include <iostream>
#include <vector>
#include <bits/stdc++.h>
using namespace std;

class Solution {
public:

        }
        if(k>a.size())
            return -1;
        else
            return a[k-1];
    }
};                                         
